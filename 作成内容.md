# 星空予想 — 作成内容

作業フォルダ: `D:\For_work01\星空予想`

星空観測・天体撮影のコンディション判定マップです。国土地理院タイルの地図に観測スポットを載せ、Open-Meteo の16日予報から星空スコアを出します。Google AdSense 審査向けに、独自解説・更新日時・計算根拠・法務ページもサイト本体に置いています。

---

## できること

- 国土地理院タイルの地図上に、観測スポットを星空スコア（★1〜5）で色分け表示する
- 日付バーで今夜から最大16日先まで切り替え、ピンの色でその夜の目安を確認する
- ピンまたはスポット名をクリックすると、詳細パネルが開く（PCは左から、スマホは下から）
- パネルにスポット解説、標高、光害、月相、月明かり、平均雲量、湿度、注意点を出す
- `update_forecast.py` を実行すると、厳選スポットの最新16日予報を `data/forecast.json` と `data/forecast.js` に書き出す
- `generate_spots_auto.py` で全国の展望台・道の駅を自動収集し、地図はマーカークラスタでまとめて表示する
- フッターから「当サイトについて」「プライバシーポリシー」「免責事項」「お問い合わせ」を開く

起動例:

```
cd "D:\For_work01\星空予想"
python -m http.server 8080
```

ブラウザで http://localhost:8080/ を開く。`file://` では JSON 取得に失敗することがあり、その場合は `js/app.js` 内のフォールバックと、同梱の `data/*.js` 埋め込みデータを使います。

---

## ファイル構成

```
星空予想/
  index.html                 画面の入口。法務モーダル（当サイトについて等）を含む
  privacy.html               privacy policy page for AdSense
  privacy/index.html         /privacy path
  css/style.css              夜空テーマ、パネル、日付バー、フッター、法務モーダル
  js/app.js                  地図・ピン・パネル・月相計算・JSON読み込み
  data/spots.json            厳選5件 ＋ 自動収集キャンプ場（標高200m以上）
  data/forecast.json         16日予報（スコア・月・雲量・湿度）。スクリプトが生成
  data/forecast.js           同上の埋め込み（file:// 用）
  data/auto_spots.json       自動収集スポット（展望台・道の駅・キャンプ場）
  data/auto_spots.js         同上の埋め込み
  data/elevation_cache.json  標高APIのキャッシュ
  data/campsite_osm_cache.json  キャンプ場Overpassの県別キャッシュ
  update_forecast.py         16日予報の更新スクリプト
  generate_spots_auto.py     OSMと国土地理院から展望台・道の駅を自動収集
  collect_campsites.py       OSMと国土地理院からキャンプ場を収集し spots.json へマージ
  ads.txt                    Google AdSense の広告主確認
  .github/workflows/update.yml  予報JSONの定期更新用ワークフロー
  作成内容.md                このファイル
```

---

## 観測スポット（spots.json）

先頭は厳選5件です。各件に 200〜300字程度の概要、視界の方角、光害と周辺、駐車場・トイレ・防寒などの注意を持たせています。

- fuji-gogome … 富士山五合目（富士スバルライン五合目、標高 2305 m、光害度 中）
- achi … 長野県阿智村（ヘブンスそのはら、標高 1400 m、光害度 低）
- nosegawa … 奈良県野迫川村（南紀の星空、標高 850 m、光害度 低）
- bisei … 岡山県美星町（美星天文台、標高 447 m、光害度 低）
- ishigaki … 沖縄県石垣島（石垣島天文台、標高 31 m、光害度 低）

その後ろに `collect_campsites.py` が集めたキャンプ場（`category: "camp_site"`）を追記しています。2026-08-24 時点で 633 件です。地図は `spots.json` と `auto_spots.json` を id で結合して表示します。

主な項目:

- name / subtitle / prefecture … 名称・補足・都道府県
- lat / lng … 緯度経度
- elevation … 標高（m）
- overview / viewDirection / surroundings / accessNotes … 解説・方角・周辺・注意
- lightPollution … 光害度（低 / 中 / 高）
- starScore … 初期スコア（予報生成後は forecast.json 側が優先）

自動収集スポットは名称・標高・光害・施設フラグから解説文を組み立てます。予報JSONと結合しても、spots.json 側の概要文は消えません。

---

## 地図と画面（フロントエンド）

- 地図ライブラリ: Leaflet.js 1.9.4（CDN）＋ Leaflet.markercluster
- タイル: 国土地理院（標準地図 / 淡色地図 / 航空写真）
- ピン色: ★5 金、★4 水色、★3 緑、★2 橙、★1 赤
- ヘッダー: 「デモデータ」は出さない。予報の取得日時を `YYYY/MM/DD HH:mm 更新` で表示
- 詳細パネル: 標高、光害度、月相、月明かり、平均雲量（1箇所のみ）、湿度メーター、解説、注意、時間帯別雲量、16日カレンダー
- データ読み込み順: `data/spots.json` と `data/auto_spots.json` を id で結合し、予報は `data/forecast.json` を重ねる。失敗時は `window.HOSHIZORA_DATA` の埋め込み、最後に app.js 内フォールバック
- `category: "camp_site"` の地点はパネルに「キャンプ場」バッジを出す

### 月相・月明かり

予報JSONの日次値（moonPhaseName / moonIllumination）を優先します。無い地点でも、選択日付の21時（JST）から朔望月 29.530588853 日と既知新月（2000-01-06 18:14 UTC）で月齢と輝面比を計算します。

表示名の目安: 新月、三日月、上弦の月、十三夜、満月、十八夜、下弦の月、有明月。輝面比は例 `78%`。

### 今夜の平均雲量

パネル上部の数値欄に1つだけ出します（メーターとの二重表示はしない）。

- **取得元**: Open-Meteo Forecast API の1時間値 `hourly=cloud_cover`
- **対象時間**: 日本時間 20時・21時・22時・23時・0時・1時・2時・3時（8時間）
- **計算**: その8個の雲量（%）の算術平均を四捨五入
- **別の日付を選んだとき**: ラベルを「この夜の平均雲量」に切り替え、同じ8時間で平均
- **予報が無い地点・日付**: `0%` などの仮値は出さず、「予報なし」と表示。説明文「この地点には選択した夜の気象予報がありません…」も出す。月相は日付から計算、スコアは標高と光害の静的目安
- **直近7日**: 同じ8時間を時間帯別バーでも表示。8日目以降は日次の総合判定のみ

湿度は同じ8時間の `relative_humidity_2m` 平均です。予報があるときだけメーターを出します。

---

## 当サイトについて（計算根拠）

ヘッダー／フッターの「当サイトについて」に、目的・使い方・項目説明・計算式を載せています。

内部点 S（100点満点）:

```
S = 雲量点 + 月照率点 + 月の出没点 + 光害点 − 湿度減点
```

- 雲量点（0〜50）: `50 × (1 − 平均雲量% ÷ 100)`
- 月照率点（0〜28）: `28 × (1 − 輝面比% ÷ 100)`
- 月の出没点（0〜12）: `12 × (月が地平線下の時間 ÷ 8時間)`
- 光害点（0〜20）: `max(0, 20 − max(1, Bortle−1) × 3)`。光害度 低／中／高は Bortle 3／4／6 相当
- 湿度減点（0〜10）: 夜間平均湿度 65%未満は0、65%以上で3、75%以上で6、85%以上で10

星への換算: ★5=80点以上、★4=62点以上、★3=44点以上、★2=26点以上、未満が★1。

輝面比:

```
輝面比% = round( (1 − cos(2π × 月齢 ÷ 朔望月)) ÷ 2 × 100 )
```

---

## フッター・法務

- プライバシーポリシー専用URL: https://starry-forecast.com/privacy.html（/privacy と /privacy/ も同じ本文。旧 #privacy と ?privacy は専用ページへ遷移）
- AdSense: index.html の head に公式ローダー（ca-pub-2075840815269276）。詳細パネルの広告枠は ins.adsbygoogle を出し、描画後に dsbygoogle.push({}) する。ルート ds.txt あり
- 免責事項: 予報は目安であること、現地の安全と天候急変は自己責任
- お問い合わせ: ただいま準備中（フォームURLは未掲載）
- 運営者情報: サイト名、個人運営、国内、コンテンツ概要

スマホではフッター分だけチップと凡例を上げ、法務モーダルは画面下から開きます。

---

## UI/UX（2026-08-24）

公開前のスマホ表示と、パネル表示時に日付が操作できない問題を直しています。

### 日付バーの前面固定

- ヘッダーと日付バーの `z-index` を詳細パネルより上（1100）に固定
- PCではパネルを日付バーの下（概ね 148px）から開始し、スポットを開いたまま別の日付へ切り替えられる
- スマホでシートを広げたときも、日付バーはシートの上に残る

### スマホのボトムシート

- 画面幅 768px 以下では、スポット詳細を画面下からのボトムシートにする
- 初期表示は画面の約 30%。スポット名・当日の星空スコア・「詳細を見る」・右上の ✕ だけを出す
- 上スワイプまたは「詳細を見る」で展開し、16日予報・施設情報をスクロール表示
- 展開中に下スワイプすると簡易表示へ戻る。簡易表示でさらに下スワイプ、または ✕ で地図全体に戻る
- 地図を隠す暗いオーバーレイは使わず、地図の視認性を確保する

### 16日予報のコンパクト化

- 16日分は縦に伸ばさず、横スクロールのカード列にする
- 各カードは日付・スコア★・雲量アイコン（晴 / 薄雲 / 厚雲）・月齢のみ
- パディングと文字を小さくし、パネルの縦スペースを圧縮

---

## 予報更新スクリプト（update_forecast.py）

### 実行方法

```
python "D:\For_work01\星空予想\update_forecast.py"
```

追加パッケージは不要です（Python 標準ライブラリのみ）。

### 処理の流れ

1. `data/spots.json` を読み込む（厳選地点とキャンプ場を含む）
2. 各スポットの緯度経度で Open-Meteo Forecast API を呼ぶ
   - 時間ごと: `cloud_cover,relative_humidity_2m`
   - 日次: `moon_phase,moonrise,moonset`
   - タイムゾーン: Asia/Tokyo、`forecast_days=16`
3. 「今夜」の開始を 20:00 とする（現在時刻が 0〜3時台なら、すでに始まっている昨夜を今夜とする）
4. 16夜分について、8時間平均の雲量・湿度、月相、月の出没比から内部点と ★1〜5 を出す
5. 概要・方角・注意もコピーして `data/forecast.json` と `data/forecast.js` へ書き出す

GitHub Actions（`.github/workflows/update.yml`）でも `forecast.json` と `forecast.js` を更新する想定です。

---

## スポット自動収集（generate_spots_auto.py）

```
python "D:\For_work01\星空予想\generate_spots_auto.py"
```

- Overpass API で都道府県ごとに `tourism=viewpoint` と、名称に「道の駅」を含む `highway=rest_area` / `highway=services` を取得（0.5秒間隔）
- 国土地理院の標高API（getelevation.php）で標高を補完
- 名称なし、展望台で標高200m未満を除外
- 標高800m以上は bortle_scale=3、それ以外は4。1000m以上は winter_closure=true
- 既存の spots.json と距離・名称で重複排除して `data/auto_spots.json` に出力

一部の県だけ試す場合:

```
python "D:\For_work01\星空予想\generate_spots_auto.py" --prefectures JP-20,JP-29
```

---

## キャンプ場収集（collect_campsites.py）

```
python "D:\For_work01\星空予想\collect_campsites.py"
```

全国の星空向きキャンプ場を OpenStreetMap と国土地理院から集め、既存の道の駅・展望台と重ならないように `data/spots.json` へ追記します。地図表示用に `data/auto_spots.json` / `data/auto_spots.js` にも同じ件をマージします。

### 取得

- Overpass API で都道府県ごとに、日本国内の `tourism=camp_site` かつ `name` がある node / way を取得
- 失敗した県は自動でもう一度取り直す
- 結果は `data/campsite_osm_cache.json` に県別キャッシュ

### 重複排除（道の駅・展望台との重なり防止）

- 名称に「道の駅」を含むキャンプ場は除外
- 既存 `spots.json` および `auto_spots.json` と同名なら除外
- 既存スポットから直線距離 500m 以内は同一敷地とみなして除外

### 品質・星空フィルタ

- 国土地理院標高API（`https://cyberjapandata2.gsi.go.jp/elevation/value`、失敗時は getelevation.php）で標高を取得
- 市街地の光害を避けるため、標高 200m 未満は除外
- `bortle_scale`: 1200m以上=2、700m以上=3、300m以上=4、その他=5
- `winter_closure`: 1000m以上=true
- `category` は `"camp_site"`。トイレ・駐車場・24時間は OSM タグから付与

### 実行結果（2026-08-24）

- OSM 候補（名称あり・重複整理後）: 1661
- 名称一致で除外: 42
- 500m 以内で除外: 122
- 標高 200m 未満で除外: 863
- 採用してマージ: 633 件
- `update_forecast.py` は `spots.json` の 638 件（厳選5 + キャンプ633）をエラーなく `data/forecast.json` / `data/forecast.js` へ更新済み

一部の県だけ試す場合:

```
python "D:\For_work01\星空予想\collect_campsites.py" --prefectures JP-20,JP-29
```

---

## 外部サービス

- 地理院タイル: https://maps.gsi.go.jp/development/ichiran.html
- Open-Meteo: https://open-meteo.com/en/docs
- Leaflet: https://leafletjs.com/

出典表示は地図右下の国土地理院リンクで行っています。

---

## GitHub へ上げても画面が変わらないとき

この作業フォルダ（`D:\For_work01\星空予想`）には `.git` がありません。GitHub の画面へ ZIP やドラッグで置いただけだと、PC 上の最新ファイルは自動では乗りません。また [github.com](https://github.com/) のリポジトリ一覧を見ても、公開サイトそのものではありません。

### 公開サイトを見る場所

GitHub Pages を有効にすると、次のような URL になります。

`https://（ユーザー名）.github.io/（リポジトリ名）/`

設定: リポジトリ → Settings → Pages → Build and deployment → Source を **Deploy from a branch**、Branch を `main`（または `master`）、フォルダ `/ (root)`。

ルートに `.nojekyll` を置いてあります。Jekyll が `data/` や JS を無視しないようにするためです。

### このフォルダから送り直す手順

Git が入っている PC で、リポジトリ URL を自分のものに差し替えて実行します。

```
cd "D:\For_work01\星空予想"
git init
git add .
git commit --trailer "Co-authored-by: Cursor <cursoragent@cursor.com>" -m "Update stargazing map"
git branch -M main
git remote add origin https://github.com/USER/REPO.git
git push -u origin main
```

すでに remote がある場合は `git add .` → `commit` → `push` だけです。フォルダごとネストして上げると `index.html` がサイトの一番上に来ないので、リポジトリ直下に `index.html` がある状態にしてください。

### 予報が自動で変わらないとき

`.github/workflows/update.yml` が `data/forecast.json` と `data/forecast.js` を更新します。

1. Actions が有効か（Settings → Actions → General）
2. ワークフローの権限が **Read and write** か（同じ画面の Workflow permissions）
3. スケジュールは新規リポジトリではすぐ動かないことがあるので、Actions タブから **Update Forecast Data** を **Run workflow** で一度手動実行する
4. 失敗ログで `git push` が Permission denied なら、上記の write 権限が原因です

### ブラウザに古い画面が残るとき

GitHub Pages とブラウザは JS を強くキャッシュします。強制再読み込み（Ctrl+F5）するか、URL に `?v=` を付けて開いてください。`index.html` の CSS / JS 参照にもキャッシュ用のクエリを付けてあります。
